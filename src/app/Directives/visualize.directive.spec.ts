import { VisualizeDirective } from './visualize.directive';

describe('VisualizeDirective', () => {
  it('should create an instance', () => {
    const directive = new VisualizeDirective();
    expect(directive).toBeTruthy();
  });
});
